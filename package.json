{
  "name": "bem-ts",
  "version": "0.5.0",
  "description": "BEM class names generator for TypeScript",
  "author": "Masafumi Koba <ybiquitous@gmail.com>",
  "repository": "ybiquitous/bem-ts",
  "bugs": "https://github.com/ybiquitous/bem-ts/issues",
  "homepage": "https://github.com/ybiquitous/bem-ts#readme",
  "keywords": [
    "BEM",
    "classname",
    "TypeScript"
  ],
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "!*test*"
  ],
  "scripts": {
    "prebuild": "rimraf dist/",
    "build": "tsc",
    "build:watch": "npm run build -- --watch",
    "pretest": "npm run build",
    "test": "jest --coverage",
    "test:watch-private": "jest --watch",
    "test:watch": "npm-run-all --parallel build:watch test:watch-private",
    "test:coverage": "npm test -- --coverageReporters=html",
    "lint:ts": "tslint --project .",
    "lint:ts:fix": "npm run lint:ts -- --fix",
    "lint:md": "markdownlint --ignore node_modules --ignore CHANGELOG.md \"**/*.md\"",
    "lint": "npm-run-all --print-name --print-label --parallel lint:*",
    "commitmsg": "commitlint -e $GIT_PARAMS",
    "precommit": "lint-staged",
    "release": "standard-version",
    "release:dry-run": "npm run release -- --dry-run"
  },
  "devDependencies": {
    "@types/jest": "22.1.3",
    "jest": "22.4.0",
    "rimraf": "2.6.2",
    "tslint": "5.9.1",
    "typescript": "2.7.2",
    "ybiq": "1.4.1"
  },
  "lint-staged": {
    "*.ts": [
      "tslint --fix",
      "git add"
    ],
    "*.md": "markdownlint --ignore CHANGELOG.md"
  },
  "standard-version": {
    "message": "chore(release): new version %s"
  },
  "jest": {
    "coverageThreshold": {
      "global": {
        "branches": 100,
        "functions": 100,
        "lines": 100,
        "statements": 100
      }
    }
  }
}
