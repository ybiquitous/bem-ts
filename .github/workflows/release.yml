name: Release

on:
  pull_request:
    branches: [main]
    types: [closed]

jobs:
  release:
    uses: ybiquitous/.github/.github/workflows/nodejs-release-reusable.yml@f3aa9016849a3bebe447aa3e1ebee363742ab0be # v3.0.0
    permissions:
      contents: write
      id-token: write
