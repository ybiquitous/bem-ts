name: Release

on:
  pull_request:
    branches: [main]
    types: [closed]

jobs:
  release:
    uses: ybiquitous/.github/.github/workflows/nodejs-release-reusable.yml@f9cf8214a0fb9118e83f115f48eac6b3d797248a # v1.0.1
    permissions:
      contents: write
      id-token: write
