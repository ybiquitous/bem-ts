name: Dependabot auto-merge

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  auto-merge:
    uses: ybiquitous/.github/.github/workflows/dependabot-auto-merge-reusable.yml@d6ef2db54793a5799605c9660e049270dcb25500 # main
    permissions:
      contents: write
